# CCPE submission source

This directory is flattened for Wiley's free-format submission. The manuscript is a one-column LaTeX article and may be uploaded with the PDF as the main document and the source files as supporting LaTeX files. The process-separated permission-service experiment is same-host loopback only; it is not evidence about WAN or multi-node deployments. Run `build.sh` to rebuild the PDF.
